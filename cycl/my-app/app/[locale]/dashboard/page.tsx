import ApplicationDashboard from "@/components/application/ApplicationDashboard";

// The dashboard is the primary landing after login — it renders the
// applications overview (cards with payment receipt, admit card and details).
export default function DashboardPage() {
  return <ApplicationDashboard />;
}
